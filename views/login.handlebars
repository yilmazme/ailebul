<div class="navbar navbar-light bg-light fixed-top">
    <a href="/people" class="btn btn-info btn-sm">Hepsini Gör</a>
    <a href="/" class="btn btn-info btn-sm">Ana Sayfa</a>
</div>
{{#if success_msg}}
<strong style="color: green;">{{success_msg}}</strong>
{{/if}}
{{#if error}}
<strong style="color: red;">{{error}}</strong>
{{/if}}
{{#if error_msg}}
<strong style="color: red;">{{error_msg}}</strong>
{{/if}}

<div class="form mt-2 p-5">
    <form action="/people/login" method="POST">
        <div class="form-group">
            <label for="name">Kulanıcı İsmi:</label>
            <input type="text" name="username" class="form-control" autofocus required>
        </div>
        <div class="form-group">
            <label for="password">Şifre:</label>
            <input type="password" name="password" class="form-control" required>
        </div>

        <button class="btn btn-success btn-sm">Giriş</button>
    </form>
    <div class="imgDiv">
         <img src="../animals/catwhy.jpg" alt="cat">
    </div>
  
</div>
{{>footer}}
<script>
    //alert("hola")
</script>
